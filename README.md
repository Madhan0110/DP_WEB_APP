# DPWebApp

**Live App**: https://dpwebapp-farccgsfm85xlmgjkfgrmq.streamlit.app/

DPWebApp is a simple data processing tool built with Streamlit for working with CSV and Excel files. It runs in the browser and is free to use.

## Features
- Upload CSV or Excel files
- Clean and preprocess data
- Export results

  
## Diabetes Prediction Project (SVM) 

1. Loaded CSV dataset using Pandas
2. Checked missing values
3. Replaced zero values in medical columns (like BMI, BP)
4. Handled outliers using IQR
5. Performed feature scaling (StandardScaler) — important for SVM
6. Split data into train/test
7. Encoded categorical data if any
8. Built model and deployed it using the streamlit
   
